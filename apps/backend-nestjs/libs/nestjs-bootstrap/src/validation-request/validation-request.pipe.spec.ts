import { ValidationRequestPipe } from './validation-request.pipe';

describe('ValidationRequestPipe', () => {
  it('should be defined', () => {
    expect(new ValidationRequestPipe()).toBeDefined();
  });
});
