import { LoginPhoneResponseDto } from './login-phone.response.dto';

describe('LoginPhoneResponseDto', () => {
  it('should be defined', () => {
    expect(new LoginPhoneResponseDto()).toBeDefined();
  });
});
