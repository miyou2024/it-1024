import { LoginEmailRequestDto } from './login-email.request.dto';

describe('LoginEmailRequestDto', () => {
  it('should be defined', () => {
    expect(new LoginEmailRequestDto()).toBeDefined();
  });
});
