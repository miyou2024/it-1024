import { LoginAccountRequestDto } from './login-account.request.dto';

describe('LoginAccountRequestDto', () => {
  it('should be defined', () => {
    expect(new LoginAccountRequestDto()).toBeDefined();
  });
});
