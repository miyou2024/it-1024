import { LoginAccountResponseDto } from './login-account.response.dto';

describe('LoginAccountResponseDto', () => {
  it('should be defined', () => {
    expect(new LoginAccountResponseDto()).toBeDefined();
  });
});
