import { LoginEmailResponseDto } from './login-email.response.dto';

describe('LoginEmailResponseDto', () => {
  it('should be defined', () => {
    expect(new LoginEmailResponseDto()).toBeDefined();
  });
});
